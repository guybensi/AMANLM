# ── Backend API URL ────────────────────────────────────────────────────────────
# Local development: leave unset — the Vite dev server proxies /api to localhost:8000
# VITE_API_URL=

# Production (Vercel): set this to your Railway backend URL
# VITE_API_URL=https://your-app.up.railway.app/api
