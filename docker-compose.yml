services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Persist the vector-store cache across container restarts
      - cache_data:/app/cache
    restart: unless-stopped

volumes:
  cache_data:
