# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Settings / env
pydantic-settings>=2.4.0
python-dotenv>=1.0.0

# LLM (Groq via OpenAI-compatible API)
openai>=1.51.0

# Embeddings
# Install CPU-only torch first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
sentence-transformers>=3.1.0
numpy>=2.0.0

# PDF processing
PyMuPDF>=1.24.0

# Image OCR (requires Tesseract binary: https://github.com/UB-Mannheim/tesseract/wiki)
Pillow>=10.0.0
pytesseract>=0.3.10

# DOCX processing
python-docx>=1.1.0

# Multipart file upload
python-multipart>=0.0.12

# Token counting for chunking
tiktoken>=0.7.0
